# E2E Full Sweep — Progress Tracker

## Plan Location
- **Full plan:** `~/.claude/plans/reactive-sprouting-wadler.md` (1,477 lines)
- **Test box:** VM 5005 (10.25.255.55) — freq installed from dev repo (`/tmp/pve-freq-dev`)
- **Config:** Generated by `freq init --headless` (no hosts file, discovery mode)

## Tier Status

| Tier | Name | Status | Session |
|---|---|---|---|
| 0 | Init from scratch (NO --hosts-file) | DONE — 22 hosts, 0 fail | S027 |
| 1 | Core CLI (utilities, fleet, host, VM) | DONE — all pass | S027 |
| 2 | Devices (net/fw/store/hw) | DONE — infra-dependent results | S027 |
| 3 | Security/observe/state | DONE — vault, boundaries, doctor | S027 |
| 4 | Auto/DR/media/gitops/RBAC | DONE — 18 containers, 7 hosts | S027 |
| 5 | Dashboard API | DONE — auth, POST, config security all pass | S027 |
| 6 | Dashboard data quality (29 pages) | NOT STARTED |

## Bugs Found

### Init (Tier 0)
| # | Bug | Fixed? |
|---|---|---|
| 1 | `--uninstall` skips remote hosts when FREQ SSH keys missing | No |
| 2 | Device-creds TOML parser fails on `[switch:cisco]` (tomllib) | **Yes** (commit a2664c9) |
| 3 | `--hosts-file` import calls .conf parser on TOML files | **Yes** (commit a2664c9) |
| 4 | Init marks initialized with garbage hosts (downstream of #3) | Fixed by #3 |
| 5 | TOML values had embedded double quotes (downstream of #3) | Fixed by #3 |
| 6 | TrueNAS deployer can't create accounts (middleware-managed) | No |
| 7 | Init discovery misses pfSense (10.25.255.1) | No |
| 8 | Init discovery misses production TrueNAS (10.25.255.25) | No |
| 9 | Init discovery misses known-offline BMC (10.25.255.12) | No |
| 10 | `--fix` treats undeployed hosts as unreachable, won't deploy | No |
| 11 | TrueNAS middleware doesn't propagate SSH keys for new users | No |
| 12 | Init registers same host twice from different VLANs (dup label) | No |

### CLI (Tier 1)
| # | Bug | Fixed? |
|---|---|---|
| 13 | `init --check --json` outputs ANSI, not JSON | No |
| 14 | `fleet status --json` outputs ANSI, not JSON | No |
| 15 | `host list --json` outputs ANSI, not JSON | No |
| 15b | `vm config` can't find VMs on remote nodes | No |
| 16 | `vm clone` fails silently with empty error message | No |
| 17 | Global `--yes` flag ignored by VM subcommand prompts | No |

### Pattern: --json flag broken on fleet/host/init commands (systematic bug)

### Dashboard + API (Tier 5)
| # | Bug | Fixed? |
|---|---|---|
| 18 | 44/56 API endpoints unauthenticated (including admin/destructive ops) | No |
| 19 | Login returns "Unknown user" for valid users (freq-admin, freq-ops) | No |
| 20 | Wrong password returns HTTP 200 instead of 401 | No |
| 21 | Empty password returns HTTP 200 instead of 401 | No |
| 22 | Dashboard runs HTTP despite TLS cert generated by init | No |
| 23 | Config API exposes notification credential fields unauthenticated | No |

### Regression Checks (62 bugs from deep audit)
| Bug # | Area | Status | Notes |
|---|---|---|---|
| 1 | Rollback crash | INCONCLUSIVE | Endpoint not found (/api/vm/rollback → 404) |
| 5 | Password escaping | PASS | Hashed server-side, no shell exec |
| 7 | Duplicate _esc() | PASS | Exactly one function |
| 15 | TrueNAS midclt | PASS | Uses midclt for create, not userdel |
| 24 | API --skiplock | PASS | Not present |
| 31 | Login DEBUG msg | PASS | No DEBUG in login HTML |
| 33 | SSE auth | PASS | Returns 403 without auth |
| 34 | Fleet 4s delay | PASS | Only toast fade uses setTimeout |
| 38 | Empty .catch() | **FAIL** | 2 empty catch handlers in media tags code |
| 41 | Operator=Admin perms | **FAIL** | Identical action lists in fleet-boundaries |
| 44 | Circular imports | PASS | No errors |
| 47 | API imports serve privates | **FAIL** | _find_reachable_pve_node imported in 8 places |
| 50 | GET for destructive ops | **FAIL** | snapshot, resize, power, rename, migrate all use GET |
| 59 | --json flag read | PASS | Added per-subcommand |
| 60 | host edit fallthrough | **FAIL** | Falls to "Unknown host action" |

### Full Regression Results: 40 PASS, 20 FAIL, 1 inconclusive (62/62 checked)

**Still broken (20) — status after S027 fixes:**

### Fixed in S026 (commits a2664c9–80cefe6):
Items 1-17 in resume state (TOML parser, API auth, POST enforcement, --json, etc.)

### Fixed in S027 (commit 77634f6):
- #8: --fix now saves generated password to vault ✅
- #53: Container matching uses exact names ✅
- #55: VM create cleans up on partial failure ✅
- #45: Duplicate find-node functions consolidated → pve.py ✅
- #46: Deployer return types already consistent ✅ (verified)
- #47: api/vm.py no longer imports serve.py privates ✅
- #51: Setup wizard has threading.Lock ✅
- #3: Dead GPU fallback code removed ✅
- #30: Gluetun uses configured port ✅
- #43: [[monitor]] added to freq.toml.example ✅
- #62: store zfs → store pools ✅
- Dashboard #22: HTTPS when TLS certs exist ✅
- Dashboard #23: /api/config strips sensitive fields ✅
- Init #1: --uninstall bootstrap key fallback ✅
- Init #6/#11: TrueNAS deployer uses midclt properly ✅
- Init #7/#8/#9: Discovery offers ping-only host registration ✅
- Init #10: --fix includes unreachable hosts ✅
- Init #12: Discovery deduplicates by hostname ✅
- Init #15b: vm config finds VMs on remote nodes ✅

### Previously fixed in S026 (already verified):
- #20: iDRAC/switch deploy — fixed (commit 80cefe6)
- #22: vm resize protected VMID check — fixed (commit 31e3c76)
- #40: Clone permission check — fixed (commit 9c1c7b9)
- #41: Operator≠admin permissions — fixed (commit 9c1c7b9)
- #50: POST enforcement — fixed (commit 1927bbf)
- #54: VMID collision check — fixed (commit 31e3c76)
- #36: Literal \n in logs — fixed (commit 9c1c7b9)
- #38: Empty .catch handlers — fixed (commit 9c1c7b9)
- #60: host edit fallthrough — fixed (commit 9c1c7b9)

### All 26 priority items: RESOLVED

## Ground Truth Verified
- Fleet status: 20 up, 5 down — matches direct SSH
- PVE VM count: 32 — exact match with PVE API
- Docker containers: arr-stack 10 containers — exact match
- Host info (pve01): kernel, cores, RAM all match
- freq-admin: 18/18 hosts verified via FREQ SSH keys
- Docker group: 7/7 hosts verified

## VM 5005 Setup Notes (for rebuild)
1. Clone tpl-debian-13 (9000) from pve03, full clone, migrate to pve01 os-drive-hdd
2. 4 NICs: vmbr0,tag=2550 / vmbr0,tag=25 / vmbr0,tag=10 / vmbr0,tag=5
3. Cloud-init: freq-ops user, fleet key, gateway on VLAN 5 only
4. VPN route: `ip route add 10.25.100.0/24 via 10.25.255.1 dev eth0`
5. Install: git, sshpass, python3-pip, then `pip install -e .` from dev repo
6. Creds: fleet key, SSH config, device-creds TOML (switch:freq-ops, pfsense:root)
7. NO --hosts-file — let init discover from PVE API
