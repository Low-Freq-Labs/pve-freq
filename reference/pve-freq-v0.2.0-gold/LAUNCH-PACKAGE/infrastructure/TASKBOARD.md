# TASKBOARD — DC01

> Last updated: S078 (2026-03-11) — pfSense firewall sweep 42→35 rules. `freq pf sweep` feature design written. FREQ Idea Checker process added. S077: Live migration proven, PDM deployed (VM 811), fleet password simplified to `changeme1234`. Lessons #128-#132.

---

## Active Tasks

| Priority | Task | Status | Notes |
|----------|------|--------|-------|
| **CRITICAL** | TICKET-0006: SSH keys + credential rotation | OPEN **44 sessions** | **S077: Fleet password simplified back to `changeme1234`** on ALL root + svc-admin accounts (14/14 SSH hosts verified). S076's 24-char rotation reverted per Sonny — security lockdown deferred to FREQ. **iDRAC NOT rotated** — complexity policy requires special chars (Lesson #128), old password still on both iDRACs (R530 .10 + T620 .11). SSH keys still deployed fleet-wide (S075). SSHD match blocks deferred — Sonny wants password SSH everywhere for now. Remaining: iDRAC password (Sonny manual), personal account passwords, SSHD match blocks (deferred to FREQ). |
| ~~CRITICAL~~ | ~~F-S043-ROOT-ADMIN: pfSense root/admin same hash~~ | **RESOLVED S049** | Root password changed and synced to config.xml. Hashes now differ. Credential rotation still tracked under TICKET-0006. |
| **HIGH** | H-007: Review Mamadou NAT rule | OPEN S034 | Exposes Proxmox 8006 to internet via 69.65.20.62. **Sonny decision.** |
| ~~HIGH~~ | ~~F-S034-NFS-ACL: ha-proxmox-disk NFS~~ | **RESOLVED S058** | Widened to 10.25.25.0/24 (storage VLAN). Any VM/node can mount. pve01 verified. |
| ~~HIGH~~ | ~~WANDR: Eliminate boot script — test /32 on igc0~~ | **DONE S060** | /32 on igc0 works. Default route naturally on lagg1. Boot script simplified to host-route only (for pf reply-to ARP). WANGW monitor changed to 8.8.8.8. 14/14 checks passed across 3 reboots. Lessons #84-#87. |
| ~~HIGH~~ | ~~WANDR DR interface + Gateway Group~~ | **DONE S059/S060** | igc0 assigned as opt7, IP 100.101.14.3/32 (was /28), WANDRGW, VIP 69.65.20.57, firewall rule, DR WireGuard verified. Gateway Group `WAN_FAILOVER` operational. **Remaining:** LANDR (igc1) assignment. |
| ~~HIGH~~ | ~~F-S059-WANDR-ROUTE: Default route depends on boot script~~ | **RESOLVED S060** | /32 eliminates competing connected route. Boot script only adds host route for pf reply-to ARP. Gateway Group Tier 1 operational. |
| ~~HIGH~~ | ~~F-S034-GPU: VM 301 GPU passthrough — amdgpu probe error -22~~ | **RESOLVED S056** | vendor-reset DKMS installed on pve03. amdgpu loaded, `/dev/dri/renderD128` present, UVD+VCE working. **GPU HW transcoding active (S058)** — h264_vaapi at 4.25x realtime. |
| ~~MEDIUM~~ | ~~F-S043-SMB-ACL: TrueNAS SMB no hosts_allow~~ | **RESOLVED S057** | hostsallow set to [10.25.0.0/24, 10.25.25.0/24, 10.25.100.0/24] via midclt API. |
| ~~MEDIUM~~ | ~~F-S044-PFSENSE-MGMT-PING: Persist ICMP rule via WebGUI~~ | **RESOLVED S060** | pf `flags S/SA` is TCP-only — ICMP passes through existing WG rules implicitly. No separate ICMP rule needed. Lesson #88. |
| ~~MEDIUM~~ | ~~M-011: Switch SSH auth~~ | **RESOLVED S061** | svc-admin SSH to 10.25.255.5 works. Cisco 4948E-F, IOS 15.2(4)E10a. Running-config accessible. |
| MEDIUM | F-S057-PKG-REPO: pfSense addon pkg repo version mismatch | OPEN S057 | `pkg update` fails: "packages for wrong OS version: FreeBSD:16:amd64". Core repo works. `pfSense-repo-setup` doesn't fix. Netgate-side issue — monitor for repo update. |
| ~~LOW~~ | ~~F-S056-VENDOR-RESET: Rebuild vendor-reset DKMS for kernel 6.17.13~~ | **RESOLVED S057** | pve03 rolled back to 6.17.9-1-pve (matching pve01). vendor-reset module loaded and working. Meta-package held. |

---

## Pending Sonny Decisions

| Item | Status | Details |
|------|--------|---------|
| SSH key deployment + credential rotation | **CRITICAL (TICKET-0006)** | 44th session. **S077: Fleet password simplified back to `changeme1234`** — all root + svc-admin on 14/14 SSH hosts. S076's 24-char rotation reverted per Sonny. SSH keys deployed for all 4 probe accounts (S063/S075) + svc-admin RSA 4096 to all 15 hosts (S075). **iDRAC passwords NOT rotated** — complexity policy (RAC947, Lesson #128), old password still works on R530+T620. Sonny: set iDRAC password manually via web UI. SSHD match blocks deferred — Sonny wants password SSH everywhere for now, FREQ will handle lockdown later. Remaining: iDRAC password, personal account passwords. |
| pve02 cluster membership | ASSESSED S035 | Recommendation: REMOVE. Procedure at `infra/TICKET-0008-PVE02-ASSESSMENT.md`. pve02 now fully compliant (S061) but cluster membership still questionable. |
| Mamadou NAT review | OPEN S034 | Who is Mamadou? Still need access? Proxmox 8006 exposed to internet. |
| Bazarr subtitle providers | NEW S045 | Connected to Radarr/Sonarr but no providers enabled. Needs OpenSubtitles account. |
| Order PSU parts | **URGENT** | R530 PSU + fan, T620 PSU. No power redundancy. |
| wsdd.py evaluation | ASSESSED S057 | wsdd.py is auto-launched by SMB service — no independent toggle in TrueNAS. Low risk since SMB hostsallow now restricts access. Cannot cleanly disable without editing TrueNAS internals. |
| Undocumented VM 420 | UPDATE S073 | VM 420 "DonnyisGay" stopped (8GB alloc). Purpose unknown. Inventory or remove? VM 802 identified S073: **Vaultwarden** (password vault, off limits). VM 804 identified S073: **Talos Linux** (K8s experiment, off limits). Both 800-series: NOT ours, do not touch. |
| ~~TrueNAS sudoers post-update check~~ | **RESOLVED S073** | **PERMANENTLY FIXED:** 59 sudo commands per probe account migrated to TrueNAS middleware DB via `midclt call user.update`. Middleware writes commands into `/etc/sudoers` as its own config — cannot wipe what it generated. Removed `@includedir`, sudoers.d file, and cron band-aid. No more session-start checks. Lesson #124. pfSense sudoers at `/usr/local/etc/sudoers.d/` — survives firmware but verify after updates. |
| ~~rsync movie migration~~ | **COMPLETED S067** | 284GB transferred, 82 empty dirs cleaned, source folder nuked (including 29GB 720p rejects). Fully migrated to `/mnt/mega-pool/nfs-mega-share/media/movies/`. |
| ~~rsync TV migration~~ | **COMPLETED S068** | ~330GB transferred. Source has 1.7GB remaining (7 unmapped folders + Mando S03 + 720p rejects). Destination: 2.9TB at `/mnt/mega-pool/nfs-mega-share/media/tv/`. rsync no longer running. |
| ~~Tdarr VM 301 resource upgrade~~ | **DONE S067** | VM 301: 4→8→12 cores. GPU-only transcode (4 GPU + 4 HC). CPU transcode disabled (CRF 23 only 2-3% compression vs GPU QP 25 at 47-68%). Node: `sj1hHTXvK`, name: `Radeon-RX580|12Core`. Stale nodes cleaned from DB. |
| Tdarr plugin overhaul monitoring | MONITORING S072 | **S072 overhaul:** VAAPI v1.5→v1.6 (HEVC ≤1080p skip — was growing files, Lesson #123). CPU v1.3→v1.4 (AV1 skip + HEVC skip + `-map -0:t`). upperBound 105→115%. 25 errors root-caused: 14 HEVC ≤1080p, 1 AV1, 2 HEVC extras, 1 sample, 7 large H264. 15 auto-resolved "Not required", 10 re-queued. **Monitor:** 8 large H264 BluRays (5-21GB) may re-error. "sample" in foldersToIgnore. |
| DR VPN route (69.65.20.57→100.101.14.3) | WAITING ON PAUL | Paul (BGP engineer) understands PBR architecture, acknowledged WANDR use case. Offered IPv6 alternative (Sonny's ISP doesn't support). Deferred actual route creation ("figure that out later"). **Follow-up needed.** |
| ~~Helper tooling on VM 100~~ | **DEPLOYED S070** | SSH config IPs corrected, quick-check.sh validated (all green, zero failures), api-keys.env verified. 7 training runs: helper improved 7/10→10/10 (37 calls/8.5min → 5 calls/1:45). |
| ~~NIC VLAN swap (VMs 101/102/104/301)~~ | **DONE S070** | net0: VLAN 5→2550, net1: VLAN 2550→5. /etc/network/interfaces swapped, PVE configs updated, all rebooted. Guest agent now shows .255 as primary. All services verified post-swap. Lesson #121. |
| ~~Huntarr swaparr + hunt cycles~~ | **FIXED S068** | S067 fix was incomplete — `url` field stored but code reads `api_url` (empty string). S068: Deleted old instances, re-POSTed with correct `api_url` + `hunt_missing_enabled: true`. Sonarr hunting AFV S29, Radarr searching Kingpin, Swaparr processing 2017 items. Root cause: Lesson #110. |
| Agregarr collection setup | PENDING S067 | Connected to Plex/Overseerr/Radarr/Sonarr. Hub configs syncing. But `collectionConfigs: []` — no TMDB/Trakt collections. **Requires WebUI** at 10.25.255.31:7171. API create endpoint exists but schema undocumented. |
| Vikings German DL replacement | DEFERRED S067 | 83 episodes (S05-S06) are "German.DL" (dual-language). English audio present but German primary. "Non-English Title" CF (ID:4) now blocks NEW German.DL grabs (S068). Existing 83 eps still need manual replacement if desired. Sonny deferred — not burning NZB quota. |
| ~~Stale completed downloads cleanup~~ | **RESOLVED S068** | 46 seeding torrents removed from qBit with data. Complete dirs 610→130GB. ZFS 17.0TB free. qBit `removeCompletedDownloads=true` prevents future accumulation. Remaining files are active downloads that auto-clean on import. |
| ~~7 unmapped TV folders~~ | **RESOLVED S068** | All 6 renamed + imported. Breaking Bad 100%, Mr. Robot 100%, Boba Fett 100%, Mandalorian 50% (S03 external), IT Crowd 38% (DVDRip partial), American Pickers 0% (empty folder). Sonarr IDs 30-35. |
| ~~Pokémon Sonarr queue entries~~ | **REMOVED S068** | 178 entries bulk-removed from queue (no blocklist — naming issue, not release issue). Pokémon/Pocket Monsters series naming incompatible with Sonarr auto-matching. |
| SABnzbd usenet providers | UPDATE S075 | **Newshosting ADDED** as primary backbone (pri 0, 30 connections — was 8, bumped S075 cont). FrugalUsenet (pri 1, 50 conn, backup). Blocknews (pri 25, 50 conn, **739/750GB quota — nearly empty**). `direct_unpack=1` enabled. 342 items were in par2 Checking (CPU-bound bottleneck on vm201). Kim Possible S01+S04 still missing (high retention needed). |
| ~~SABnzbd WebUI auth~~ | **RESOLVED S075** | Enabled via API (`mode=set_config`): username=admin, password=temp1234. Auth method: `apikey` (basic auth). No container restart needed — took effect immediately. |
| PVE vzdump backups | NEW S073 | **All 3 PVE nodes have empty vzdump configs — zero automated VM backups.** No scheduled backups, no storage target configured. Needs backup strategy (what to back up, where, how often). |
| Security findings (helper S094) | UPDATE S074 | 5 remaining vulnerabilities (1 RESOLVED S074): ~~(2) pve02 `PermitRootLogin yes`~~ → **RESOLVED S074:** All 3 PVE nodes now `PermitRootLogin prohibit-password` via dc01-hardening.conf. Remaining: (1) Plex unauth API on mgmt VLAN, (3) DNS bypass (all hosts use external DNS directly, pfSense Unbound not in path), (4) WG VPN full access to all 6 VLANs, (5) NFS broad /24 subnet exports, (6) No fail2ban anywhere. Kill chain reduced but still viable. Needs Sonny prioritization. |
| SSHD match blocks for probe accounts | **DEFERRED** | Disable password auth per-account. **All 4 probe accounts now have SSH keys deployed fleet-wide (S075).** Sonny wants password SSH everywhere for now — FREQ will handle lockdown later. |
| **MEDIUM** | Asymmetric routing fix (10 hosts) | OPEN S078 | VPN clients (10.25.100.x) can reach mgmt IPs but NOT LAN/Public IPs. Replace global static route `10.25.100.0/24 via 10.25.255.1` with source-based policy routing on 10 hosts (3 PVE + 4 public VMs + 3 dirty VMs). Start with VM 104 as guinea pig. Per-host commands in `pfsense-post-sweep-buildout-handoff-3-11-26.md` Task 1. |
| **MEDIUM** | VLAN 10 GigeNet employee buildout | OPEN S078 | Repurpose VLAN 10 (Compute) as GigeNet employee VLAN. Delete C2, keep C1, add 9 isolation rules (gold standard pattern). **Requires pfSense writes + physical access.** PHP templates ready in buildout handoff Task 2. After VLAN 10 live: set up Mamadou WG peer, delete Mamadou NAT. |
| **MEDIUM** | LAN SSH/WebUI lockdown | OPEN S078 | Block SSH (22) and WebUI (80, 443, 4443, 8006) from LAN to internal VLANs. Insert 4 block rules between L1 and L2. **Requires pfSense writes + physical access.** Design alongside VLAN 10 buildout. Details in buildout handoff Task 3. |
| **LOW** | DR emergency WireGuard config | OPEN S078 | Create `dc01-emergency.conf` pointing to 100.101.14.3:51820 (DR WAN). Client-side only, zero risk. Details in buildout handoff Task 4. |

---

## Completed (Recent)

| Session | What |
|---------|------|
| S078 | **pfSense firewall rule sweep (42→35) + FREQ feature design + Idea Checker process.** Full 9-interface audit: 5 rules deleted, 1 hardened (W6 any→ICMP-only), 1 renamed. Zero downtime. `freq pf sweep` feature design written (1,046 lines). FREQ Idea Checker process added to CLAUDE.md §4. 6 config backups on pfSense. 4 buildout tasks deferred (asymmetric routing, VLAN 10, LAN lockdown, DR WG). |
| S077 | **Live migration + PDM deployment + fleet password simplification + Tdarr health check.** VM 802 round-trip pve01↔pve03 (24-61ms). Fleet CPU standardized to `x86-64-v2-AES`. PDM deployed as VM 811 (7/7 tests). Fleet password simplified back to `changeme1234` (14/14 hosts). VM 999 snapshot deleted. Template 910 fixed. Tdarr: 7,235 files, 1,890 GB saved. Lessons #129-#132. |
| S076 | **CRIT-04 resolved + fleet password rotation + WireGuard peer visibility.** API keys moved from docker-compose.yml to `.env` + recyclarr `secrets.yml` on vm102 (4 locations, 11 containers verified). Fleet password rotation 13/13 SSH hosts (root + svc-admin to new master). `freq vpn wg show` built — all WireGuard peers with ACTIVE/IDLE/NEVER status. iDRAC password rotation blocked (complexity policy, old password documented). Lesson #128. |
| S075 | **Probe permission expansion + SSH key fleet deployment + vm202 onboarding + svc-admin hardening + SABnzbd tuning.** Probe sudoers expanded on all 7 Docker VMs. SSH keys (ed25519) for probe accounts + RSA 4096 for svc-admin deployed fleet-wide. VM 100: all 4 probe accounts. vm202 onboarded. pfSense sudoers redeployed. SABnzbd WebUI auth. Dead indexers disabled. BatchMode=yes. **Continuation:** svc-admin RSA 4096 key to all 15 hosts (13 SSH + 2 iDRACs). R530 iDRAC unblocked (privilege wipe). svc-admin sudo 15/15 verified (vm400 fixed). VM 400 SSH locked to mgmt VLAN + WG return route. SABnzbd Newshosting 8→30 conn + direct_unpack. Lessons #126-#127. |
| S074 | **PVE SSH hardening + Tdarr cleanup + VM 102 migration.** dc01-hardening.conf standardized on all 3 PVE nodes (PermitRootLogin prohibit-password, dual ListenAddress for mgmt+cluster). 47 corrupt media files deleted via Sonarr/Radarr APIs, re-searches triggered (1080p only). Tdarr HC errors 47→0. VM 102 online-migrated to pve02 (os-pool-ssd, 64GB). PVE datacenter.cfg migration network set to .255 VLAN. pve02 PermitRootLogin security finding resolved. Lesson #125. |
| S073 | **TrueNAS sudoers permanent fix + fleet cron audit + helper testing + LTS 2.0.** TrueNAS sudoers migrated to middleware DB (59 commands per probe account, Lesson #124) — permanently fixed after 3 wipes. Fleet cron audit: only non-standard was TrueNAS band-aid (removed). pve02 dead xfs_scrub_all.timer cleaned. PVE vzdump empty flagged. Permissions doc + permission-test.sh updated/deployed. **Helper:** 8 tests graded A+ (9.75/10). Helper S094: Radarr loopers fixed (8 deleted, 9 unmonitored, 112→0 blocklist) + 6 new security findings. Helper S095: LTS 2.0 rewrite (30 files, 16 hosts verified live, 31 severity-tagged active issues). VM 100: 2c/4GB→4c/8GB. VM 802=Vaultwarden, VM 804=Talos identified. |
| S072 | **Tdarr plugin overhaul + sudoers expansion + password sweep + permissions doc.** Tdarr: VAAPI v1.5→v1.6 (HEVC ≤1080p skip, Lesson #123), CPU v1.3→v1.4 (AV1+HEVC skip, `-map -0:t`), upperBound 105→115%, 25 errors→0 (15 "Not required", 10 re-queued), 100 stale staged cleared, "sample" in foldersToIgnore. **Sudoers expanded:** TrueNAS (+crontab -l, +zpool get), PVE x3 (+zpool get), pfSense (wg path `/usr/local/bin`→`/usr/bin` fix, +arp -an, +ping -c with flood blocked). **Permissions doc** created at `/mnt/obsidian/guides/jarvis-ai-permissions.md` (460 lines, API section corrected after helper review). **VM 400 SSH alias** (vm400/runebot) on VM 100. **Password sweep:** 3 drifts fixed (donmin@pve01, chrisadmin+donmin@TrueNAS) — 44/44 verified. Helper backup reviewed (110 files/0 errors). Helper LTS 1.5 audit reviewed (10 doc fixes, zero infra changes). |
| S071 | **Helper review + sudoers verification + screen install + test prompt tuning.** Reviewed helper's Tdarr+Radarr fix session (B+/A-). docker restart sudoers verified on all 6 Docker VMs. screen installed on VM 100 via PVE guest agent. Helper test prompt created (fleet roll call). 2 helper test runs reviewed (12/13 hosts, missed VM 400 wrong IP; systems check B+). ip-allocation.md verified. Anthropic outage noted (intermittent). |
| S070 | **NIC VLAN swap + Helper tooling + DR VPN discussion.** VMs 101/102/104/301 net0 VLAN 5→2550 (guest agent now shows .255 as primary). VM→IP mapping confirmed via QEMU guest agent (VM 104=.33 resolved "mystery .33"). Helper tooling on VM 100: SSH config IPs corrected, quick-check.sh bugs fixed (Huntarr endpoint, loop targets, totalRecords grep), full validation run all green. 7 helper training runs (7/10→10/10). TrueNAS @includedir wiped 3rd time, re-added. DR VPN: Paul (BGP) acknowledged PBR architecture, deferred route. Lessons #121-#122. |
| S069 | **Sonarr queue cleanup + Tdarr plugin v1.5 + Kim Possible Hebrew fix + SABnzbd pre_check.** Sonarr queue 195→24 (Pokémon/Family Guy/Bones). Huntarr unstuck + `command_wait_attempts` 600→120 (30min failover). SABnzbd 4 warnings cleared. **Season 0 specials unmonitored** (13 series, 839 phantom eps, missing 3179→2885). **Tdarr:** 2802 files, 430 transcoded, 555.8GB saved. Plugin v1.4→v1.5 (AV1 skip, 10-bit software decode, `-map -0:t`). CPU worker 0→1. 37 errors reset + fresh rescan. **Prowlarr:** 3 indexer backoffs cleared (EZTV/TPB/Darkpeers), all 5 healthy. **Kim Possible:** 42 Hebrew episodes deleted (S01+S04, D00oo00M group). D00oo00M added to "Non-English Title" CF (ID:4) on both arrs. English replacements failed (Frugal retention exceeded, 0 torrents). **SABnzbd `pre_check` enabled** — fixes phantom queue orphans where aborted NZBs showed "Queued" not "Failed", blocking arr auto-redownload pipeline (Lesson #120). Lessons #117-#120. |
| S068 | **Full plex stack audit remediation (9 fixes, ALL VERIFIED).** Huntarr: root cause fixed (api_url not url, Lesson #110), `low_usage_mode: false`, hourly caps 20→100. **Sonarr queue 2018→~1650:** orphans, German/NORDiC, Pokémon, stuck Family Guy, 68 SAB failures cleaned. **Radarr 5 failed cleaned.** "Non-English Title" CF (ID:4) on both arr services (Lesson #111). **EZTV re-enabled + baseUrl fixed** (`https://eztvx.to`). **7 unmapped TV folders resolved:** Mandalorian S01+S02 merged, 6 series added to Sonarr (Breaking Bad/Mr. Robot/Boba Fett 100%). **6 shows searching** (397 eps). AFV + American Pickers unmonitored. **39 German Family Guy blocklisted** (S08+S09). **qBit removeCompletedDownloads=true** on both arr services. **46 seeding torrents cleaned** (Lesson #114). Complete dirs 610→130GB. **SABnzbd:** root cause = Frugal Usenet 97.7% article failure (Lesson #115). `bandwidth_perc` 80→100, `queue_limit` 20→5. ZFS 17.0TB free. Lessons #110-#115. |
| S067 | **Plex stack hardening + rsync migration + Tdarr 4K plugin + VM 301 12-core + GPU-only transcode.** Language filters (Not English CF -10000), Plex notifications with path mapping, torrent delay 24h→30min, NZBgeek removed. **rsync movies COMPLETED (284GB), source nuked.** TV rsync in progress (~129GB moved, 223GB remaining). **Tdarr plugins v1.4:** 4K→1080p downscale. GPU HC CUDA bug fixed. **VM 301: 4→12 cores**, GPU-only transcode (4 GPU + 4 HC, CPU transcode disabled — only 2-3% compression vs GPU 47-68%). Stale node DB cleanup fixed UI flickering. 485 files in queue. **Plex:** poster fix + useLocalAssets disabled. **SABnzbd:** 1TB/193 items at 71 MB/s (MCU + Adventure Time + Kim Possible). Lessons #100-#109. |
| S066 | **pfSense WebGUI hardening + SABnzbd fix + svc-admin audit + iDRAC accounts + Tdarr pipeline overhaul.** Block-TCP-to-self rules on 4 VLANs. SABnzbd bind IP .66→.255. Sonarr/Radarr download client URLs fixed. TrueNAS @includedir re-added. Switch probe priv 1→15. svc-admin audit: 13/13 SSH + 11/11 sudo + 6/6 Docker + 3/3 PVE API. iDRAC accounts on R530+T620. **Tdarr:** plugin v1.3 (QP 25/CRF 23), h264_vaapi encoder test bug fixed (missing hardwareUtils.js entry), file size check 40→25%, Plex notification added. End-to-end test: Iron Man 12GB→3.9GB GPU transcode verified. **VM 802:** NIC bridge vmbr0→vmbr0v2550, passwords reset. **TrueNAS:** chrisadmin/donmin/sonny-aif added to builtin_administrators for Web UI. Lessons #94-#99. |
| S065 | **Switch config-register + DNS standardization + domain standardization.** Switch `write memory` saved (config-register 0x2102 staged). DNS fleet-wide to 1.1.1.1/8.8.8.8 (6 systems fixed: VM 100, VM 201, pve01-03, TrueNAS). Domain standardized to `infra.dc01` on all 12 systems. pfSense renamed fw01→pfsense01 (config.xml backed up). VM /etc/hosts stale VLAN 1 IPs corrected to actual service VLAN IPs. PVE /etc/hosts IPs aligned to mgmt VLAN (.255.x) matching corosync ring0. Lessons #92-#93. |
| S063 | **Probe account permission overhaul + VM 100 Obsidian mount + TrueNAS SMB access overhaul.** Sudoers whitelist (`dc01-probe-readonly`) deployed to all 12 hosts. SSH keypair (ed25519) generated on VM 100, pubkeys fleet-wide. adm group on 9 Linux hosts. **TrueNAS fixes:** dc01-probe group (GID 3950) isolates probe accounts from truenas_admin sudo. `smb-share-rw` group (GID 3001) — all 4 probe accounts, Share ACL set to group FULL, POSIX ACL on share root. **VM 100:** 3rd NIC (VLAN 25 storage, 10.25.25.2). Obsidian vault CIFS mount at `/mnt/obsidian` (rw, fstab persistent). Yescrypt→SHA512 password hash fix (Lesson #75). jarvis-ai password re-fixed (shell `!` escaping). hostnamectl, ntpq, bare-command sudoers fixes. 36/37 verification tests passed. Full memory drift check — 3 items fixed across DC01.md + ssh-and-credentials.md. |
| S062 | **Full memory integrity audit + 13-system infrastructure sweep + fleet cleanup + package compliance.** Fixed 20+ doc references across 5 files (all stale after S061 VM renumbering). Account GIDs normalized to 950 on 7 systems. VM 101/102 stale routes removed. Old kernels purged fleet-wide — all 7 VMs now single-kernel 6.12.73. VM 100 + VM 201 rebooted for kernel activation. PVE fleet: corosync 3.1.10 + widget-toolkit 5.1.7 on all 3 nodes. Package compliance audit: VM 201 (11 bloat removed, tcpdump/rsync added), pve02 (6 bloat removed), VM 100 (5 bloat removed). 10/10 Linux systems compliant. SMB hostsallow verified (false alarm — TrueNAS 25.10 schema). VM 201 cleanup (home dir, NTP, stale dirs). Cloud-init cleaned from pve01. |
| S061 | **PVE02 + VM 201 compliance + VM ID renumbering + Jarvis-AI VM + account rollout + infra fixes.** PVE02: accounts, packages (9.1.6), NIC naming (nic0-3), VLAN bridges, SSH hardened. VM 201 (SABnzbd) full compliance. **VM ID changes:** 105→104, 104→301, 501→201. **VM 100 (Jarvis-AI)** created on pve01: Debian 13 standard install, 2 NICs (mgmt+public), no sudo, fully compliant. **jarvis-ai account** (UID 3004) rolled out as read-only probe to all 12 hosts. donmin created on pfSense (was missing). TrueNAS: ssh-homes dataset, AllowUsers expanded, FULL_ADMIN stripped from personal accounts. PVE: jarvis-ai@pam → ReadOnly/PVEAuditor. chrisadmin/donmin passwords reset fleet-wide. **Infra fixes:** Corosync ring0, VLAN 25 split-brain, HA services, ISO NFS, switch SSH. Lessons #90-#91. |
| S060 | **WANDR /32 test + LANDR assignment + ICMP resolved + full health check.** igc0 /28→/32: default route hijack eliminated, DR WireGuard fixed with host route, WANGW monitor→8.8.8.8, boot script simplified. F-S044 ICMP resolved: pf `flags S/SA` is TCP-only, ICMP passes existing WG rules. LANDR (igc1) assigned as opt8 cold standby. 3 config.xml changes, 3 clean reboots, 14/14 checks. **Health check:** 10/10 systems up, all VMs kernel 6.12.73, MTU 9000, NFS mounted, Docker healthy, 0 updates, backups current. VM NTP fixed: all 5 VMs changed from 10.25.0.1→10.25.25.1 (storage VLAN). VM 103 VPN exit OK, VM 104 GPU OK. Lessons #84-#89. |
| S059 | **WANDR interface configured + DR WireGuard verified + Gateway Group.** igc0 assigned as opt7 (config.xml edit — WebGUI blocks /28 overlap). IP 100.101.14.3/28, WANDRGW gateway, VIP 69.65.20.57. Firewall rule UDP 51820. DR WireGuard tested — handshakes confirmed. Gateway Group `WAN_FAILOVER` (WANGW Tier1, WANDRGW Tier2) set as default gateway — pf `route-to` handles all transit routing. Boot script (`wandr_route_fix.sh`) as belt-and-suspenders for kernel route. Reboot verified: gateway group survives, brief WANGW loss at boot handled correctly, VM internet confirmed. LACP 4/4. Lessons #78-#83. F-S059-WANDR-ROUTE resolved. |
| S058 | **Iron Man fix + Arr tuning + Tdarr GPU transcoding + NFS/firewall/SSH hardening + health check.** Iron Man corrupt HEVC replaced with x264 FGT. Radarr/Sonarr/Prowlarr: custom formats (x264+15, HEVC-25), 1080p-only, min 15 seeders, NZB priority. Huntarr Swaparr enabled. Tdarr: custom VAAPI GPU + CPU plugins, AMD hwupload fix, GPU workers=2/CPU=1. NFS ACL widened to /24. pfSense inter-VLAN + mgmt lockdown audited (already solid). TrueNAS SSH locked to svc-admin only (AllowUsers). Plex remote access confirmed (5G). Full health check: all 10 systems up, 16 containers. Lessons #75-#77. |
| S057 | **Full health check + VM updates + pve03 kernel rollback + Plex cleanup + TrueNAS SMB hardening + qBit WebUI fix.** 10/10 systems up. VM fleet updated (10 pkgs across 4 VMs, kernel 6.12.73 on 101/105). pve03 rolled back 6.17.13→6.17.9 (vendor-reset working, meta-package held). Plex library cleaned (16 stale movies + 2 TV shows removed). SMB hostsallow set. VM 104 NTP fixed (storage VLAN). pfSense pkg repo mismatch noted (Netgate-side). qBit WebUI fixed — S042 policy rule 201 caused asymmetric routing for DNAT'd traffic from WireGuard; removed from runtime and /etc/network/interfaces. Lessons #72-#74. |
| S056 | **GPU passthrough fix + pve01 updates + fleet package level-set.** vendor-reset DKMS on pve03 (patched for kernel 6.17). pve01 7 packages upgraded (9.1.6). Bloat removed from all 5 VMs (11 packages) + standardized (tcpdump/rsync added). PVE nodes cleaned (6 packages). Lessons #70-#71. |
| S055 | **Full health check + NTP cascade fix + CLAUDE.md rewrite + fstab fix.** ntpd_vip_fix.sh updated (LAN+storage+mgmt listen). TrueNAS chrony + all 5 VMs syncing. VM 103 via storage VLAN (pf blocks dirty/mgmt). WSL fstab `noauto` added. Lessons #68-#69. |
| S054 | **Health check + pfSense firmware update + MTU fix.** Tdarr API key fixed. mTLS certs refreshed, sudo installed, firmware 26.03.a→26.03.b. MTU 9000 restored. lan-failover.sh recreated. Post-update runbook + Obsidian vault updated. Full health check: 3 findings (GPU passthrough, NTP binding, pve01 updates). Lessons #65-#67. |
| S053 | **JARVIS_PROD comprehensive rewrite.** All files cleaned, dual-write policy added (SMB + ~/JARVIS_LOCAL/), originals archived to archive-pre-S053/. Change log S16-S047 archived to CHANGELOG-ARCHIVE.md. |
| S052 | **Personal account level-set + WSL SMB mount simplification.** sonny-aif/chrisadmin/donmin UIDs standardized, all set read-only across all 7 hosts. PVE ACLs cleaned. WSL fstab simplified. Lesson #64. |
| S051 | **svc-admin permissions + VM fleet NIC audit + pfSense full audit.** svc-admin verified/fixed on all 7 hosts. All 5 VMs NIC-compliant. WG→Storage VLAN rule re-enabled. Lessons #60-#63. |
| S050 | **pfSense hardening + successful reboot.** All 6 S049 recovery items fixed. Unbound VIP sourcing, NTP fix script, lagg watchdog cron, svc-admin in config.xml. Clean reboot — 15/15 checks passed. Lessons #54-#59. |
| S049 | **WANDR cold standby attempt + config disaster recovery.** /32 on igc0 → reboot broke config → restored pre-S036 backup → rebuilt lagg1 WAN + svc-admin. 6 items deferred to S050. Lessons #48-#53. |

---
